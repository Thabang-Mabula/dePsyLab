export enum DecisionMatrixRoutesEnum {
  CONTEXT_ROOT = 'decision-matrix',
  TITLE = 'title',
  CRITERIA = 'criteria',
  OPTIONS = 'options',
  ASSESS_OPTIONS = 'assess-options',
  RESULTS = 'results',
}
