/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ScorePipe } from './score.pipe';

describe('Pipe: Scoree', () => {
  it('create an instance', () => {
    let pipe = new ScorePipe();
    expect(pipe).toBeTruthy();
  });
});
